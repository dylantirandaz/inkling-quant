extends:
  - tiny_moe_baseline.yaml
schema_version: "1.0"
name: tiny-moe-mps-baseline
benchmark:
  protocol_version: mps-eager-v1
runtime:
  backend: torch_eager_mps
  device: mps
  dtype: float32
  device_map: single
output:
  root: artifacts
  run_prefix: tiny-moe-mps-baseline
